#revise 
def countBits(n):
    binary = bin(n)
    
    for i in str(binary):
        count = 0
        if str(i) == 1:
            count+=1
        else:
            continue
    return count
            




        
n = int(input())
print(countBits(n))
